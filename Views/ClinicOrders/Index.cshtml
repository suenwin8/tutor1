@model IEnumerable<tutor1.Models.Entity.ClinicOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.clinicOrder_seqid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfClinicOrder)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seeDoctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdatedTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.clinicOrder_seqid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfClinicOrder)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.seeDoctor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdatedTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClinicOrderId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClinicOrderId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClinicOrderId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
